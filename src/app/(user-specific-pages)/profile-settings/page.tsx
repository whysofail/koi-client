import React, { FC } from "react";

const ProfileSettings: FC = () => {
  return (
    <div>
      Only user can access to this page, otherwise it will show 403 forbidden
    </div>
  );
};

export default ProfileSettings;
