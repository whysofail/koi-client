import React, { FC } from "react";

const page: FC = () => {
  return (
    <div>
      This is an article page, completely separated from application pages and
      publicly accessible, to add more public routes go to roleAccessMap.ts
    </div>
  );
};

export default page;
